
#basically,

# Vortex & Footprints
VORTEX_INSTALL_DIR=/path/to/vortex
PYTHONPATH=$VORTEX_INSTALL_DIR:$VORTEX_INSTALL_DIR/src:$VORTEX_INSTALL_DIR/site:$PYTHONPATH
export PYTHONPATH

# EPyGrAM
# go to site-packages directory (site.getusersitepackages())
# link EPyGrAM directory
# and copy EPyGrAM.pth therein
# add [absolute path to site-packages]/apptools to $PATH (export PATH=$PATH:...)
