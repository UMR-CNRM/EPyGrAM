# unless you have a good reason for it, let the library manage on her own:
unset GRIB_SAMPLES_PATH
unset GRIB_DEFINITION_PATH
unset ECCODES_SAMPLES_PATH
unset ECCODES_DEFINITION_PATH

# EPyGrAM
EPYGRAM_INSTALL_DIR=$HOME/.epygram/src
export PYTHONPATH=$EPYGRAM_INSTALL_DIR:$EPYGRAM_INSTALL_DIR/site:$PYTHONPATH
export PATH=$PATH:$EPYGRAM_INSTALL_DIR/apptools
